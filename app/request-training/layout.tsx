export const metadata = {
  title: "Request Training",
  description: "Request a free community first aid training from Frontline Firstaid.",
  alternates: { canonical: "/request-training" },
};

export default function RequestTrainingLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
